<div class="container-fluid">
    <div class="card col-sm-8 col-md-6 col-lg-6 m-auto">
        {{ form_start(form) }}
       {{ form_widget(form) }}
  <div class="mb-4">
    <button class="btn btn-primary">{{ button_label|default('save') }}</button>
  </div>
        {{ form_end(form) }}
    </div>
</div>
